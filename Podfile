# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Peer Review' do
  use_frameworks!
  pod 'Cosmos', '~> 12.0'
  pod 'Firebase'
  pod 'FirebaseUI'
  pod 'PureLayout'
  # Pods for Peer Review


end
